// Public builds contain no credential. Only the personal EAS Android build replaces this stub.
// An embedded credential is extractable from the APK; it is not a server-side secret.
export const personalBuildKey: string | null = null;
