# Current build status

**Active scope: Android mobile implementation authorized with Gemini free tier.** The browser/Expo Go prototype remains available for visual review; native functionality is now being moved into the standalone app.

The root `index.ts` loads `preview.ts` on web and Expo Router on Android. Web loads preview screens, shared presentation and curriculum text; Android loads the actual routes and services. See `PROTOTYPE_REVIEW.md` for browser review steps.

Available preview: photographic Home, 30 programme topics, day overview, Input/Think/Outline and visual Coach hierarchy. The Android app now includes persistent SQLite programme/prep/takes, camera recording, private video storage, free-tier policy acknowledgement, SecureStore key entry, audio-only AAC extraction, validated Take 1 Coach, correction delta, hidden follow-up, local confidence/writing/diary, progress and vocabulary views.

The underlying Android app remains incomplete. The full P0 evaluation is **not run / not passed**. No physical Android test or native audio extraction validation has passed. A live synthetic-speech Coach check passed on 16 September (details below); this does not validate human coaching quality on the target device. Native measurements, reminders, diary biometric lock, exports and remaining curriculum resilience work are still pending.

On 11 September, TypeScript and scoped ESLint/Prettier checks for the personal-build changes passed. All 37 tests across eight suites passed, followed by two additional integration checks for preconfigured-key coaching and privacy gating (39 total). Full-worktree ESLint still reports an existing follow-up render-purity error and four warnings in entry/module scaffolds. Full-worktree formatting reports 20 existing files outside this change. Browser visual results from the earlier prototype review are recorded in root `design-qa.md`; no native visual validation is implied.

The earlier Android export passed: `npx expo export --platform android --output-dir dist/mobile-android` compiled `index.ts` (1,659 modules) and emitted a 5 MB Hermes bundle plus assets. Preview APK build `0b1f4367-f53a-4ada-ad88-b32f77b08390` finished successfully on EAS. This is not a physical-device or native extraction validation.

The user requested a personal APK with an embedded Gemini key on 11 September. Source checkpoint `9acec3e` adds EAS-only injection, automatic SecureStore setup, persistent disable, and the tested `gemini-3.6-flash` default. Text generation and a synthetic WAV audio/structured-JSON request passed live API tests. The personal version 1.0.1 build succeeded and its logs confirm credential injection. On 13 September, reviewing its Expo Doctor output identified a missing direct `expo-font` dependency and SDK patch mismatches. Version 1.0.2 (Android version code 3) adds the font package/plugin and aligns packages using Expo's installer before a replacement native build. See `PERSONAL_BUILD.md` for the explicit credential-policy exception and verification limits. Do not publish the personal APK to public GitHub or web hosting.

On 16 September, both REST and the installed Gemini SDK returned `OK` using the supplied key. The manual `scripts/verify-coach.cjs` test then passed with 14-second and 20-second synthetic library-topic speech: Take 1 produced six scores, two validated corrections and a hidden follow-up; Take 2 evaluated both original correction IDs with `not fixed` and `fixed` verdicts. The test uses the actual Coach/comparison prompts and validators, with the native request limits, but sends PCM WAV rather than Android-extracted AAC. Earlier longer requests timed out or received Google HTTP 503 high-demand errors. The app now explains 503 failures as temporary provider overload and keeps the existing retry flow; it does not switch models or enable billing.

Replacement-build checks on 16 September: TypeScript passed; all 40 tests across nine suites passed (20-second runner timeout); scoped ESLint and Prettier passed for the changed config, Gemini client, manual verification script and personal-coaching integration tests. An initial preview timeout passed on isolated rerun and the final full run. The added recovery test verifies that provider failure saves no feedback, preserves the existing recording and allows a successful retry. After installing the SDK's newly recommended patches through `npx expo install --fix`, Expo Doctor passed 21/21 checks. Full-worktree lint/format limitations above remain unchanged.

The final Android Metro export passed with 1,662 modules, a 5 MB Hermes bundle and 47 assets. GitHub checkpoint `1395668` contains the replacement build's source. The personal 1.0.2 / code 3 APK was submitted to EAS on 16 September; cloud completion and artifact verification are pending. A subsequent longer live test passed 41-second Take 1 but timed out on 48-second Take 2; the successful shorter two-take smoke test must not be presented as universal service reliability. See the first-phone checklist and detailed limitations in `PERSONAL_BUILD.md`.
